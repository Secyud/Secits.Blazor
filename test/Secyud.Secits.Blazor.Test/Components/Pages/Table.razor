@page "/table"


<STable Data="_items">
    <STableColumnSetting TItem="TestView"
                         Field="@(item => item.Name)"/>
</STable>

@code {
    readonly List<TestView> _items = TestView.GetTestViews();
}