@attribute [Route(Menu.Table)]

<STable Items="Items" ValueField="u => u.Id">
    <STableColumnSetting Caption="@nameof(ListItem.Id)"
                         ValueField="@(item => item.Id)"/>
    <STableColumnSetting Caption="@nameof(ListItem.Name)"
                         ValueField="@(item => item.Name)"/>
    <STableColumnSetting Caption="@nameof(ListItem.Description)"
                         ValueField="@(item => item.Description)"/>
    <STableColumnSetting Caption="@nameof(ListItem.PhoneNumber)"
                         ValueField="@(item => item.PhoneNumber)"/>
    <STableColumnSetting Caption="@nameof(ListItem.Age)"
                         ValueField="@(item => item.Age)"/>
    <STableColumnSetting Caption="@nameof(ListItem.BirthDate)"
                         ValueField="@(item => item.BirthDate)"
                         Format="yyyy MM dd hh mm ss"/>
</STable>

@code {
    List<ListItem> Items { get; } = ListItem.Generate();
}