@attribute [Route(Menu.Card)]

<SCard Title="@Title" Text="@Content"/>





@code
{
    string Title { get; } = Utils.GetRandomWord(true);

    string Content { get; } = Utils.GetRandomSentences(10);
}
