@namespace Secyud.Secits.Blazor.Preset
@inherits SInput<TValue>
@typeparam TItem
@typeparam TValue

@{
    base.BuildRenderTree(__builder);
}

@code
{
    protected override RenderFragment GenerateSetting() => __builder =>
    {
        @base.GenerateSetting()
        <InputTextTemplate TValue="TValue" Class="w-100" Readonly @onclick="OnDropDownClickAsync"/>
        <EnableFieldInput TValue="TItem" TField="TValue" Field="Value"
                          ValueField="ValueField" ItemFinder="ItemFinder"
                          FieldChanged="ValueChanged" ValueUpdated="OnCloseDropDownAsync"/>
        <EnableDropDown @ref="_enableDropDown" Class="button icon plain">
            <SList TItem="TItem" Style="max-height: calc(var(--s-s) * 16);">
                <StaticDataSource Items="Items"/>
                <EnableIteratorVisualize/>
                <EnableIteratorSelect/>
                <ListColumn ChildContent="ListItemTemplate"/>
            </SList>
        </EnableDropDown>
    };
}