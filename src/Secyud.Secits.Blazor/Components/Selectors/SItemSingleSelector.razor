@namespace Secyud.Secits.Blazor.Components
@inherits SSelectorBase<SItemsIteratorBase<TItem>, TItem>
@typeparam TItem

@code
{
    public override RenderFragment GenerateSelectedContent() => __builder =>
    {
        if (SelectContent is not null)
        {
            @SelectContent(new(this, SelectedItem))
        }
    };
}