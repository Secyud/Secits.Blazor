@namespace Secyud.Secits.Blazor
@inherits SLayoutPluginBase<SInput<int>>

@code
{
    public override RenderFragment RenderTemplate() =>
        @<SliderBox Value="GetValue()" ValueChanged="SetValueAsync"
                    Style="@GetStyle()" Class="@GetClass()" Cycle="Cycle"
                    Disabled="Master.Disabled" Readonly="Master.Readonly"
                    Format="@Format" Max="Max" Min="Min" NumberCount="NumberCount"
        />;
}

