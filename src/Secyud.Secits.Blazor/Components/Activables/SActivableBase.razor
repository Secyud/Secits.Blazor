@namespace Secyud.Secits.Blazor
@inherits SContentBase


@((RenderFragment)base.BuildRenderTree)

@code
{
    protected override RenderFragment GenerateChildContent() => __builder => { @GenerateBody() };

    protected override RenderFragment GenerateContent() =>
        __builder =>
        {
            @GenerateHeader()
            @if (Disabled || Readonly)
            {
                <div class="@GetClass()" style="@GetStyle()" @onclick:stopPropagation>
                    @GenerateChildContent()
                </div>
            }
            else
            {
                <div class="@GetClass()" style="@GetStyle()" @onclick="OnClick" @onclick:stopPropagation>
                    @GenerateChildContent()
                </div>
            }

            @GenerateFooter()
        };
}

