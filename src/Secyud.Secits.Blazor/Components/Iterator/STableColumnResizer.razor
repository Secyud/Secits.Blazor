@namespace Secyud.Secits.Blazor
@inherits SSettingBase<STable<TItem>>
@typeparam TItem

@code
{
    public RenderFragment GenerateHeader(int index) => __builder =>
    {
        if (Master.TableColumns.Count - 1 <= index) return;

        <div class="col-resizer" @onmousedown="()=>BeginDragColumnHeader(index)"></div>
    };
}
