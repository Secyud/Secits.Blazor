@namespace Secyud.Secits.Blazor.Components
@inherits SItemSingleSelectorBase<STable<TItem>, TItem>
@typeparam TItem

@code
{
    public override RenderFragment GenerateSelectedContent()
    {
        throw new NotImplementedException();
    }

    public RenderFragment GenerateRow(TItem item, RenderFragment<TItem> content)
        => this.GenerateTableRow(item, content);
}
