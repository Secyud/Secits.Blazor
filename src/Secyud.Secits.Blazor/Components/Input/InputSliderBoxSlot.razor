@namespace Secyud.Secits.Blazor
@inherits SSettingBase<SInput<int>>

@code
{
    public RenderFragment RenderSlot() =>
        @<SliderBox Value="Master.CurrentValue" ValueChanged="Master.OnValueChangedAsync"
                    Cycle="Cycle" Disabled="Master.Disabled" Readonly="Master.Readonly"
                    Format="@Format" Max="Max" Min="Min" NumberCount="NumberCount"
        />;
}

